[tables]
show_all = show tables;
desc_table = desc {table};
create_test_table = CREATE TABLE IF NOT EXISTS {table} (
                        id INT AUTO_INCREMENT PRIMARY KEY,
                        first_name VARCHAR(50) NOT NULL,
                        last_name VARCHAR(50) NOT NULL,
                        age INT,
                        major VARCHAR(100));
[insert]
into_users = INSERT INTO users (username, email) VALUES("{user}", "seplusi_arcanjo@hotmail.com")
[select]
all_from_users = select * from users